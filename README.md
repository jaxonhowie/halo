# Halo

macOS 桌面像素风布偶猫宠物。纯 Swift + SpriteKit 实现，无第三方依赖。

## 功能

- **待机** — 猫咪坐在桌面，自然眨眼动画（12 帧，6fps）
- **走动** — 水平移动，碰壁自动转向（9 帧）
- **睡觉** — 蜷缩身体，漂浮 ZZZ 动画（4 帧）
- **想吃鱼** — 猫咪渴望吃鱼的可爱动画（11 帧）
- **跳跃** — 点击触发，带 "喵~" 文字气泡
- **拖拽移动** — 按住猫咪拖到桌面任意位置
- **右键菜单** — 手动切换状态、提醒喝水、提醒走动、查看喝水统计或退出
- **桌面置顶** — 透明无边框窗口，浮动在所有窗口之上
- **跨桌面** — 所有桌面空间可见
- **不在 Dock 显示** — 以 accessory 模式运行
- **喝水提醒** — 监控键盘输入，工作 15 分钟后弹窗提醒喝水，支持倒计时气泡和每日统计
- **走动提醒** — 工作 60 分钟后弹窗提醒起身走动，独立倒计时气泡

## 安装与启动

```bash
# 编译运行（一键）
./run.sh

# 或者分步执行
swift build
.build/debug/Halo

# Release 编译（优化）
swift build -c release

# 打包为 .app
./build_app.sh
open .build/Halo.app
```

要求 macOS 13+，Xcode Command Line Tools。

**喝水/走动提醒功能**需要辅助功能权限：系统设置 > 隐私与安全性 > 辅助功能。

## 基本操作

| 操作 | 效果 |
|------|------|
| 左键点击 | 猫咪跳跃 + "喵~" |
| 按住拖拽 | 移动猫咪位置 |
| 右键 | 打开菜单（待机/走动/睡觉/想吃鱼/提醒喝水/提醒走动/退出） |

猫咪会在待机、走动、睡觉之间自动切换状态。点击会打断当前状态触发跳跃。

## 喝水提醒

- 开始打字后自动启动 15 分钟倒计时
- 画布顶部显示带描边和阴影的倒计时气泡（💧 15:00）
- 倒计时结束后猫咪移到屏幕中央，弹出"该喝水了哟~"确认框
- 点击"喝过啦"后猫咪回到原位，今日喝水次数 +1
- 右键菜单显示"今日喝水: X 次"，每日自动重置
- 右键菜单可手动触发"提醒喝水"

## 走动提醒

- 开始打字后自动启动 60 分钟倒计时
- 独立倒计时气泡（🚶 60:00），与喝水提醒互不干扰
- 倒计时结束后猫咪移到屏幕中央，弹出"该起来走动啦~"确认框
- 点击"走过了"后猫咪回到原位
- 右键菜单可手动触发"提醒走动"

## 项目结构

```
Sources/Halo/
├── main.swift              # AppKit 入口，透明无边框窗口、事件处理、喝水/走动提醒
├── CatSpriteScene.swift    # SpriteKit 场景：状态机、动画、交互、倒计时气泡
├── SpriteData.swift        # Sprite 帧数据与颜色表（字符映射编码）
└── Resources/              # PNG 像素动画资源
    ├── idle/               # 待机动画（idle0-idle11，12 帧）
    ├── walk/               # 走动动画（walk0-walk8，9 帧）
    ├── sleep/              # 睡觉动画（sleep0-sleep3，4 帧）
    └── wantFish/           # 想吃鱼动画（wantFish0-wantFish10，11 帧）
```

## 技术实现

- **渲染** — SpriteKit，PNG 纹理以 `.nearest` 过滤保持像素锐利
- **窗口** — AppKit NSWindow，透明无边框，`.floating` 置顶，208x208 像素
- **状态机** — idle / walk / sleep / wantFish / jump，带权重随机切换
- **动画系统** — 固定时间步长（1/60s），各状态独立帧率
- **交互** — 左键点击跳跃，拖拽移动，右键菜单切换状态
- **喝水提醒** — `NSEvent.addGlobalMonitorForEvents` 监听键盘，`Timer` 计时，`NSAlert` 弹窗，`UserDefaults` 持久化喝水统计
- **走动提醒** — 独立 `Timer` 计时（60 分钟），独立倒计时气泡，独立弹窗确认
- **倒计时气泡** — 多层 `SKLabelNode` 叠加实现描边（8 方向黑色偏移）+ 阴影（右下灰色偏移）

## 致谢

- 代码由 [Claude Code](https://claude.ai/code) + Mimo-v2.5-pro 生成
- 素材图由 GPT-Image2 生成
- 剪切处理使用 [Aseprite](https://www.aseprite.org/) + [PixelLab](https://www.pixellab.ai/) 完成
